"use strict" // treat all js code as newer version
//alert(3+3) // we are using nodejs, not browser so it will not work
console.log(3+3);
console.log(typeof null) // null is a opbject
console.log(typeof undefined)
